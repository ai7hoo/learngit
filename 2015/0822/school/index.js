var klass = require('./klass');

exports.add = function(klasses){
    klasses.forEach(function(item,index){
        var _klass = item;
        var teachers = item.teachers;
        var students = item.students;
    })
    klass.add(teachers,students);
};